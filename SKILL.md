---
name: persona-auditor
description: >
  数字人审计员——从真实用户视角审计 AI 生成的代码，找出"你自己反复试验才能发现、甚至根本意识不到"的错误，
  上帝视角归因成少数根因，让你精准修复，而不是继续补丁式地瞎试。
  派"数字人"模拟真实用户、纸面穷举全部用户路径（不执行代码也能穷举，比真实点击更快更全局）。
  专治：AI 写的代码功能能跑、逻辑自洽，却反复卡在前置几步、改很多轮找不到根因。
trigger:
  - 审计
  - 测试
  - 找 bug
  - 推演
  - 验收
  - 用户旅程
  - 逻辑推演
  - 纸面推演
  - 出包前检查
  - 上架前检查
  - 发布前检查
---

# 数字人审计员（Persona Auditor）

## 一、定位与铁律

**定位**：一个**综合审计编排 skill**，目的是**精准修复**——找出"用户自己反复试验才能发现、甚至根本意识不到"的错误，归因成少数根因，让 agent 一次修对。三条腿：

1. **主线（特色）**：用户画像 → 状态机穷举 → 数字人推演 → 体验层三方对齐 → 参照组 → 上帝视角归因。抓"底层逻辑与体验层的矛盾"，专治"卡一个点改很多轮找不到根因、缺全局视角"。
2. **编排复用（不重造）**：安全 / AI 味 / 过度工程 / 上架审核 / 通用正确性，去加载执行现成 skill，结果并入统一报告。
3. **两个自研特色检查（现有 skill 都没覆盖）**：
   - **发布卫生**：AI 辅助开发特有的病——把开发试错、对话、总结性结论、真实个人信息、密钥、内部决策写进代码/配置/注释/文档。
   - **造轮子审计**：查"整个功能模块有更成熟的开源方案，却自己造了个过时的"，搜索对比后询问用户是否替换。

**纸面推演是手段，不是目的**：不执行、不点击，是为了"低成本地穷举全部用户路径"——比真实点击更快、更全局、能一眼看到根因。**但它不是"不修复"的借口**：审计的闭环是「报告（根因+精确位置）→ 用户确认 → 交给 agent 精准修复 → 用可证伪验证（修了症状消失）」。

**铁律**：
1. **不执行代码、不真实点击、不修改代码**——主线工作在纸面完成。
2. **代码是唯一真相源**：从"读代码还原的真实行为"出发，不基于设计文档或记忆脑补。
3. **不陷入"代码自洽"误区**：代码内部逻辑自洽 ≠ 产品好用。
4. **穷举是灵魂，抽样是失职**：第 2/3 步必须完整遍历「画像×功能×操作×状态×时序」矩阵，多数字人子代理各自独立推演，**遍历完所有路径后才能做根因分析**。禁止抽一条正常路径就下结论。
5. **复用优先，不重造**：安全/AI味/可维护/审核去加载 `llm-sast-scanner` / `code-auditor` / `ponytail-audit` / `code-reviewer`，不重写它们的规则。
6. **发布卫生和造轮子是本 skill 自研特色，必须查，不跳过**。
7. **读代码核对贯穿多步，不是第 1 步读一次就完事**：数字人推演（第 3 步）、极端测试（第 4 步）、三方对齐（第 8 步）、归因（第 9 步）**每一步都必须重新读代码核对，禁止凭第 1 步的记忆下结论**。每条结论的 `文件:行号` 必须是"本轮刚读到/刚 grep 到"的，不是"记忆里的"——记忆会过期、会幻觉，只有当场读到的才算证据。
8. **误报检查（报之前必须过）**：报任何问题前，先确认"这个场景适用吗"——不上架的别报商店审核、自用的别报软著合规、桌面端别报移动 App 审核、测试 fixture 别当生产 bug。**报错场景比漏报更伤信任。**
9. **什么不能碰、什么不该报**：测试 fixture / vendor 三方库（压缩后）/ 构建产物（out/dist）/ .env 模板 / 文档里的 TODO 占位——这些默认**不算问题**，除非用户明确要求审计它们。报它们是误报。
10. **找不到就如实说缺什么，禁止编造**。
11. **结论必须可证伪（科学性的底线）**：每条根因/缺陷结论，必须附带一个**可证伪的验证方法**——"如果修了 X，症状 Y 应该消失；如果修了 X 后 Y 还在，说明这条结论错了"。**禁止报"无法证伪"的结论**（如"架构有问题""代码质量差""不够好"这类空话）。没有验证路径的结论 = 不可证伪 = 不算审计发现，只算"印象"，必须降级标注为 `【印象，无验证路径】`。

---

## 二、方法论（启动问答门 + 十步）

### 启动问答门（必做，不可跳过，不可脑补）

纸面推演的"标尺"只有用户知道，**禁止 agent 自己脑补**。

**第一步：判型**。先快速读 `README` / `package.json` / 目录结构，判断项目类型（web 应用 / CLI / 库 / 桌面应用 / agent 系统 / 游戏 / 其他）。

**第二步：通用四问**（任何项目都问）：
1. **给谁用**：有几类目标用户？各自什么使用习惯、期望、容忍度？
2. **设计初衷**：每项核心功能，设计时希望用户得到什么体验？
3. **发布场景（决定合规/审核检查是否适用，不问清必误报）**：上架哪里（App Store / Google Play / 国内应用商店 / 官网分发 / 不上架）？自用还是商用？要不要申请软著？——**这直接决定"软著合规""商店审核门控"这些检查是否要做**：不上架/自用/不申请软著时，这些检查跳过，否则全是误报。
4. **审计范围**：六层全查，还是只查某几层？

**第三步：按类型追问**（判型后针对性追问，让问答门对任何项目都能提出"正确有用的问题"）：
- **web/桌面应用**：核心用户旅程是哪几条？哪步最容易让用户流失/困惑？有没有要上架的目标平台？
- **CLI/库**：目标开发者是谁？典型集成场景？API 设计上有没有刻意想保持的约束？
- **agent 系统**：人机交互模式（对话/画布/语音）？自动化边界在哪（哪些动作绝不该自动做）？
- **游戏**：目标玩家？核心乐趣是什么？有没有付费/上架计划？

**降级规则**：用户答不上来的项，从「代码 + README + 设计文档」反推，报告里标 `【反推，非用户口径，需确认】`。审计中若出现"必须用户拍板才能判断"的点（参照组 A/B 选哪个、需求合不合理、造轮子要不要替换），**停下问用户，不替用户决定**。

### 轻调研（问答门之后、第 0 步之前，必做）

**数字人不能拍脑袋生成——先做一轮轻调研，用真实数据校准画像，再编排模拟行为。** 这不是深度调研，控制在少量搜索内，只为拿到三样东西：

1. **目标用户的共同点**：搜索这类产品/场景的真实用户画像（web_search / 社区 / 应用商店评论 / 竞品测评），提炼"这类用户普遍是什么样的人、有什么习惯"。
2. **真实痛点**：这类用户用同类产品时，普遍抱怨什么、卡在哪、最痛的是什么。
3. **行为模式**：这类用户典型怎么用（一次性？高频？多任务切换？）。

**三条纪律**：① 每条调研结论**带来源 URL**；② 只搜"共同点 + 痛点 + 行为模式"，不陷入海量搜索（轻调研 = 3-5 次搜索封顶）；③ 调研结果与问答门"用户口径"冲突时，**用户口径优先**（创始人比外部调研更懂自己的产品），但冲突点要显式列出，供用户拍板。

### 第 0 步：定标尺——用户画像分层（针对性）

基于**三方合成**：问答门（用户口径）+ 轻调研（外部真实用户数据）+ 反推（代码/文档口径），产出：

- **用户画像分层表**：≥3 类用户，每类写：使用习惯 / 期望 / 容忍度 / 会用与不会用的功能 / **痛点** / **行为模式**。每条标注来源【用户口径】/【轻调研·URL】/【反推】。
- **设计初衷表**：逐项功能写"设计时希望用户得到什么体验"，标注来源。

### 第 1 步：还原真相——读代码建模状态机

- 还原**真实状态机**：状态变量（含初始值）、转移条件、分支、边界、竞态点。
- 还原**每项功能的真实行为**（代码真的做了什么，而非文档说的）。
- 标注：代码实际 vs 设计初衷 的偏差。

### 第 2 步：穷举路径——角色组合 + 组合复用原则（穷举 ≠ 笛卡尔积全测）

**铁律：穷举 = 完整遍历「角色组合 × 功能 × 操作 × 状态 × 时序」，但"组合"用等价类 + 边界值 + 正交数组，不测笛卡尔积全量。禁止只抽一条正常路径就下结论。**

**组合不遗漏的方法论（软件测试经典三件套，保证"不遗漏 + 不爆炸"）**：

1. **等价类划分**：每个维度先划分等价类——用户画像按"行为趋同"分（不是无限细分）；功能按"同类行为"归组（所有"建卡"是一类，不必每张卡都单测）；操作按"正常/异常/边界/中断"四类，每类选代表值（空输入、超长、乱序各一个代表，不逐个字符测）。
2. **边界值分析**：对数值/长度/数量类边界，测**边界两侧**（0 和 1、上限和上限+1、空和 1 个字符）——bug 集中在边界。
3. **正交数组（降维不遗漏）**：不用笛卡尔积全测，用正交表保证"**任意两个维度的组合至少被覆盖一次**"。三因素以上才用（画像×功能×操作），两因素直接全组合。

**覆盖率要量化可证明**：穷举完，报告里必须给出——`覆盖 N 类画像 × M 组功能 × K 类操作 = P 条路径；正交表保证任意两维组合至少覆盖一次；边界值覆盖 0/1/上限/上限+1`。**"不遗漏"不是嘴上说，是用等价类+边界值+正交数组的数学性质保证，并写出覆盖数。**

**角色 = 画像 × 场景的组合**（不是单一画像，也不是画像×场景的笛卡尔积全测）：

1. 先列**单场景角色**：每类画像 × 每个场景（如 小白×vibe coding、小白×漫剧、高手×vibe coding、高手×漫剧…）。
2. 再列**组合场景角色**：真实用户往往是"既做 A 也做 B"（如 小白既做 vibe coding 也做漫剧）。
3. **组合复用原则（关键，避免组合爆炸）**：
   - 若"单做 A"和"单做 B"都各自测过、没问题，则"既做 A 又做 B"的**组合角色不重测全部**，只测**跨场景增量**：
     ① **精准跳转**：A↔B 之间切换是否精准、可逆、不留错状态；
     ② **跨项目记忆召回**：记忆池里是否知道 A 和 B 的关系，能否回答"我之前在 A 项目做过 X"；
     ③ **动态记忆池是否生效**：跨项目记忆是否正确召回、不串台、不丢。
   - 只有"单场景本身没测过"或"组合会引入新状态"时才全测组合。

**数字人数量有依据，不拍脑袋**：数字人总数 = 需要的角色组合数，**设上限 ≤10**（省 token + 契合 agent 最大并发数）。超出上限时，按"先单场景、再高价值组合"排序取舍，并在报告里说明舍弃了哪些组合、为什么。

### 第 3 步：确定性数字人推演 + 上帝视角（不是混沌模拟，token 可控）

**关键区分**：我们做的是**确定性推演**——代码是确定的、状态机是确定的，不是 MiroFish 那种"社会演化/群体涌现"的混沌推理。所以数字人数量 = 第 2 步的**角色组合数**（上限 ≤10，不是上千），token 可控。

1. **派数字人子代理（prompt 有依据，不拍脑袋）**：为第 2 步的每个角色组合，各派一个子代理（"数字人"）。**数字人 prompt 必须由三方合成**：①轻调研（外部真实用户共同点+痛点+行为模式，带 URL）②问答门（创始人设计初衷+目标用户）③代码反推（功能清单+状态机）。prompt 固定字段：`身份 / 使用习惯 / 期望 / 容忍度 / 痛点 / 行为模式 / 会用与不会用的功能 / 本角色要推演的路径清单`。**每个数字人独立推演，不互相参考；组合角色的数字人只测跨场景增量，不重测已覆盖的单场景。**

2. **每个数字人逐条沿完整代码链路核对**：对它的每条路径，从「用户动作 → 入口函数 → 状态机转移 → 工具执行 → 落盘 → 输出 → 新状态」**逐环节读代码核对真实行为**，记录每一步：`实际走到了哪个分支 / 状态怎么变 / 输出了什么 / 断在哪`。**禁止看到某行像 bug 就标记——必须沿完整链路走通，才能说"这条路径在 X 处断"。**

3. **预测 + 因果（预测必须闭环到代码证据）**：每个数字人不仅记录"断了"，还要**预测**：这个用户做完这一步后，下一步会怎么做、会看到什么、产生什么误解。**每条预测必须落到"代码哪一行导致了这个结果"**——预测没有代码证据的，标 `【推测，未定位】`，禁止把"编故事"当"推演"。

4. **数字人还要检测这些维度（不只是"能不能跑通"）**：
   - **可发现性**：这个用户怎么发现某功能？找不到怎么办？
   - **可恢复性**：做错了能不能撤销/恢复？报错信息能不能看懂？
   - **信任建立**：用户凭什么相信 agent 做对了？证据/反馈在哪？
   - **心智模型一致性**：用户以为的和系统实际做的一致吗？（重点，对应"代码自洽但体验不对"）
   - **放弃点**：这个用户在哪一步会放弃/离开？为什么？
   - **隐私感知**：这个用户对"我的数据去哪了"的感知和恐惧。

5. **分歧显式化**：不同数字人对同一路径的判断可能不同（小白觉得卡住、高手觉得正常、专家觉得太啰嗦）——**这种分歧本身就是"体验层落差"的证据**，必须显式记录"哪个数字人怎么判断、为什么分歧"，不得抹平。

6. **数字人结果汇总**：等**所有数字人的所有路径**都推演完，先做一轮"数字人层"的汇总——哪些断点是单个用户专属（边界）、哪些是所有用户都会踩（系统性）、数字人之间的分歧在哪。**注意：这里只汇总，不做全局归因——全局归因是第 9 步"上帝视角"的事，那时所有错误（不止数字人，还有六层检查）才到齐。**

### 第 4 步：极端测试——越狱注入 + 压力并发

- **越狱/注入**：提示词注入、角色劫持、系统伪装、越狱模板、不可见 Unicode。对照第 1 步的"可信输入边界"，找穿透点与正确拦截点。
- **多任务并发压力**：同时跑 N 个任务、同卡并发、竞态、超时、打断、重复提交、快速切换。推演谁覆盖谁、锁是否失效、队列是否串行化。

### 第 5 步：专项检查编排——复用现成 skill（不重造）

去加载执行下面现成 skill，把结果带回并入报告：

| 维度 | 复用 skill | 查什么 |
|---|---|---|
| 安全性 | `llm-sast-scanner` | 34 类漏洞 Source→Sink 污点追踪 |
| AI 味 / 可维护性 | `code-auditor`（AI味节）+ `ponytail-audit` | AI 命名空洞/注释套路/异常吞噬/TODO坟场；过度工程 delete/stdlib/native/yagni/shrink |
| 上架审核 | `code-auditor`（软著+商店门控节） | **条件触发**：仅当问答门"发布场景"明确要上架/申请软著时才做；自用/不上架/不申请软著时**跳过本行**（否则全是误报）。且要先认清平台：Electron 桌面端不走 App Store/Google Play 移动审核，报"移动商店 4.2/低质"是误报 |
| 通用正确性 | `code-reviewer` | 正确性/安全/可维护/性能 🔴🟡💭 |

重复项以更专的为准，去重后合并。**每一层报结果前，先过铁律第 8 条"误报检查"：这个场景适用吗？不适用就不报，标注"该层因发布场景不适用，跳过"。**

### 第 6 步：发布卫生检查（自研特色，必做）

**AI 辅助开发特有——把不该公开发布的东西写进了代码/配置/注释/文档。** 逐项扫：

- **真实个人信息**：姓名/电话/邮箱/地址/学校/成绩/身份证等，硬编码进代码、配置、fixture、注释。
- **密钥与凭证**：API key / secret / token / 私钥 / 个人工作区端点，写进代码（尤其 .env 之外的地方）。
- **开发试错痕迹**：注释里的"实测事故""用户说XX""审计发现XX""走查缺口N""临时/先这样/待删"等内部过程记录。
- **内部对话与总结性结论**：AI 把开发时的对话、试错结论、内部决策写进 prompt/注释/文档（如"验收会逐条核""必须派子代理"这类私人意图）。
- **内部领域信息**：内部项目名、私人域名、内部架构决策、未公开的账号体系。

每发现一条，标 `【发布卫生】+ 文件:行号 + 内容类别 + 泄露等级`，并在报告里单列。**这类问题不在现有任何 skill 的检查范围内，本 skill 独家覆盖。**

### 第 7 步：造轮子审计（自研特色，必做）

**查"整个功能模块有更成熟的开源方案，却自己造了个过时的"。**

1. 枚举项目的功能模块/自研依赖（尤其"看起来像自己手写"的：鉴权、状态管理、消息队列、ORM、模板引擎、工具函数集、协议实现…）。
2. 对每个疑似自研模块，**先搜索**（web_search / npm view / GitHub search）有没有成熟开源方案，对比：成熟度、维护活跃度、功能完整性、许可证是否可商用不侵权。
3. **代码对比**：现有自研实现 vs 成熟方案，差在哪（画蛇添足？技术过时？功能缺失？维护成本高？）。
4. 发现"明显不如成熟方案"的，**停下问用户是否替换**，不擅自决定（可能用户有刻意自研的理由）。

### 第 8 步：体验层三方对齐 + 参照组

- **三方对齐**：每项功能摆三样——①设计初衷 ②代码实际行为 ③用户真实预期，找落差，重点抓"代码自洽但体验不对"。
- **参照组试验**：对"需求本身是否合理"存疑的功能，设计 A/B 对照，纸面推演两种设计各自给用户什么，对齐需求、降沟通成本。

### 第 9 步：上帝视角全局归因（最后一步，所有错误到齐后才做，科学归因）

**这不是数字人推演的收尾，是整个审计的最终归因——必须等前面所有步骤（数字人推演 + 六层检查 + 三方对齐 + 压力测试）的错误全部收集齐，再统揽全局。**

**归因方法论（区分"相关"和"因果"，不是把相似的堆一起）：**

1. **收集错误全集**：把前面所有步骤发现的错误，汇总成一张"错误全集表"（每条带 文件:行号 + 触发条件 + 症状）。

2. **聚类候选根因**：按"共同代码位置 / 共同触发条件 / 共同机制"聚类。每个聚类是一个**候选根因**（注意：此时只是"候选"，尚未验证）。

3. **反事实验证（关键，区分根因和症状）**：对每个候选根因，做反事实推理——"**如果修了这一处，这个聚类里的错误是否一起消失？**"
   - 一起消失 → 它是**真根因**；
   - 只消掉一个、其余还在 → 它只是**症状**（或"共同表象"），真正的根因在别处。
   - 反事实推演要写出来：`候选根因 X → 假设修掉 X → 推演：A/B/C 是否随之消失？`

4. **画因果链（不是平铺罗列）**：`根因 → 中间因 → 症状`。多个症状可能追溯到同一根因；一个症状可能有多个促成因，要标"必要/充分"：
   - **必要条件**：缺了它故障不发生（但有了不一定发生）；
   - **充分条件**：有了它故障必发生。

5. **根因分层 + 影响面排序**：按"一个根因能消掉多少症状"排序优先级（影响面），而不是按单个症状的严重度排序。**报告最终只突出"少数几个根因"，而不是几十个症状**——这才是"大局观"。

6. **每条根因附可证伪验证方法（必做）**：每个根因结论，必须写清"**怎么验证它是真的**"——`假设根因 X 为真 → 修掉 X → 预期症状 A/B/C 一起消失；若修后仍在，则 X 不是根因，需回退重归因`。这是归因结论的落地闭环：审计不是终点，修完验证才是。**凡没有验证路径的根因，标 `【印象，无验证路径】`，不得当结论。**

### 第 10 步：出报告——六层归类 + 归因后的收敛方向

把六层结果 + 第 9 步的归因，统一输出。报告主体是六层归类，但**结论部分必须是"归因后的少数根因 + 按影响面排序的收敛方向"**，不是症状清单。

---

## 三、输出格式（六层归类）

```
# 数字人审计员报告

## 0. 用户画像分层表 + 设计初衷表（标来源：用户口径/反推）
## 1. 状态机还原 + 与设计初衷的偏差
## 2. 穷举矩阵（五维：画像×功能×操作×状态×时序，完整遍历）
## 3. 多数字人推演结果 —— 逻辑层（每个数字人独立推演 + 预测因果，附每条路径的完整链路核对记录）
## 4. 极端测试结果 —— 越狱穿透点 / 并发竞态点
## 5. 专项检查结果
     - 安全层（llm-sast-scanner）
     - AI味/可维护层（code-auditor + ponytail-audit）
     - 合规层（code-auditor 软著+商店门控）
     - 通用（code-reviewer）
## 6. 发布卫生检查 —— 发布卫生层（本 skill 特色）
## 7. 造轮子审计 —— 架构层（本 skill 特色，含"是否询问替换"）
## 8. 体验层三方对齐 + 参照组 —— 体验层
## 9. 上帝视角全局归因（反事实验证后的根因，不是症状清单）
     - 错误全集（去重后）
     - 候选根因聚类 + 反事实验证（"修掉它，哪些症状一起消失"）
     - 因果链：根因 → 中间因 → 症状（标注必要/充分）
     - 根因影响面排序（一个根因消掉多少症状）
## 10. 收敛方向（按根因影响面，不是按症状严重度）
```

每条缺陷统一带：`触发序列 → 预期 vs 实际 → 根源 → 归属层 → 严重度`。**结论部分必须是"归因后的少数根因 + 影响面排序"，不是几十个症状平铺。**

---

## 四、能力边界

- **擅长**：逻辑矛盾、流程卡死、状态污染、分支遗漏、路径边界、体验层落差、越狱穿透、并发竞态、AI 味、过度工程、上架拒件、**发布卫生（AI 特有）**、**造轮子（成熟方案对比）**。
- **审计阶段只读，修复在闭环里**：审计时**不改代码**（为了不污染现场、不影响正在跑的东西），但审计产出的是"少数根因 + 精确文件:行号 + 可证伪验证方法"，**用户确认后，交给同一个 agent 按报告精准修复，再验证"症状是否消失"**。这是完整的「审计 → 确认 → 修复 → 验证」闭环，审计只是第一棒。
- **局限**：代码实际行为若与"你以为的行为"不一致，纸面推演只能发现"规格层矛盾"。缓解：发现"推演 ≠ 实测"时把实测补回状态机。
- **不能做**：不能读二进制/逆向（只能读明面源码）、不能替代真实 UI 视觉检查、不能替代真实执行回归（这后两样交给视觉工具 / ux-toolkit / agent-qa）。本 skill 是"定位 + 大局观 + 编排 + 特色检查"工具，修复由 agent 在用户确认后执行。

---

## 五、何时用、何时不用

- **用**：开发越久模糊点越多、反复卡在前置几步、要验收、出包前、上架前、发布前、要判断"需求本身对不对"、要一份六层全覆盖的综合审计。
- **不用**：需要真实 UI 视觉检查、需要真实执行回归、纯性能基准、只想做单一维度（直接调对应专项 skill）。
